import EnseignantLayout from "@/components/EnseignantLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <EnseignantLayout>{children}</EnseignantLayout>;
}
